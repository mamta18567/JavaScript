const fruits = ["Apple","Mango","Orange"];
let [var1,var2,...myArray]=fruits;
console.log(var1,var2);
console.log(myArray);

// If we want to store a values of an array into variable then array destructuring is used.Here we make a new array and keep variables inside that ans store the the value of an array into it.