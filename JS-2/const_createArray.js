const fruits = ["Apple","Mango","Orange"];
fruits.push("Banana");
console.log(fruits);
// whenever creating an array using const you can only push and pop elements into an array but we cannot change the whole array like:
// fruits = ["strawberry"];-----> wrong